on:
  push:
    # Sequence of patterns matched against refs/tags
    tags:
        - '*' # Push events to matching * ex : v1.0, 20.15.10

name: Create Release

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false
